# 依赖
node_modules/

# 打包产物（安装包过大，不入库；发布时上传到 GitHub Releases 附件）
dist/

# 运行日志与临时文件
*.log
npm-debug.log*

# 系统/编辑器杂项
.DS_Store
Thumbs.db
desktop.ini

# Office 临时锁文件（~$ 开头的由 Office 打开文档时生成）
~$*

# ---------- 不入库的敏感内容 ----------
# 激活码计算工具（含机器码/激活码算法与远程激活页面），单独私有保存，禁止提交
license-tools/
license-tool-gui/

# 命令行版激活码工具（复用 main/license.js 算法，同样不得公开）
scripts/license-tool.js

# 激活密钥文件：算法模块运行时读取，密钥本身绝不入库
main/license-secret.json

# 登录凭据文件：正常落在运行时用户目录（userData/data），此处为防御性忽略，
# 避免在仓库目录内调试生成时被误提交（内含加密密码密文，同样不应入库）
credentials.json

# 网页版激活工具（含密钥与算法，供管理员本地打开，禁止发布）
docs/activate.html

# 代码加密方案文档（仅本地留存，泄露即等于公开混淆配置）
# 注意：模式必须与实际文件名一致，此前写成「源代码加密说明.md」匹配不上
# 真实文件「源代码加密方案.md」，导致该文档被提交并推送到公开仓库
源代码加密方案.md
源代码加密说明.md
*加密方案*.md
*加密说明*.md

# 网页版适配层（尚未定稿，暂不入库）
web/

# 本机开发工具（截图流水线 / 行为测试 / 发布脚本，含本机绝对路径，不入库）
开发工具/
