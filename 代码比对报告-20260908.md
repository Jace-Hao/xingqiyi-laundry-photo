# 本地代码 vs GitHub 仓库代码比对报告

**比对时间**：2026-09-08  
**本地路径**：`E:\软件开发\星期衣精致洗衣衣物照片系统`  
**GitHub 仓库**：https://github.com/Jace-Hao/xingqiyi-laundry-photo.git

---

## 一、文件概览

### 仅存在于本地（不在 GitHub 仓库中）

| 文件/目录 | 说明 |
|---|---|
| `星期衣产品发布页-20260905T064636377Z/` | 产品发布页文件夹（含历史版本、素材等） |
| `星期衣精致洗衣衣物照片系统-操作手册-v0.1.0.docx` | 操作手册文档 |
| `scripts/tmp-create-release.ps1` | 临时发布脚本 |
| `待修复及增加功能.md` | 本地备忘录 |

### 仅存在于 GitHub（本地没有）

| 文件 | 说明 |
|---|---|
| `.gitignore` | Git 忽略规则文件 |

### 内容完全相同

| 文件 | 状态 |
|---|---|
| `renderer/assets/logo.png` | ✅ 一致 |

---

## 二、有差异的文件详情（共 19 个）

> 结论：GitHub 仓库版本整体比本地更新，新增了较多功能和改进。

### 🔧 main 目录（主进程，5个文件均有差异）

#### main/main.js（本地 47,527B → 远程 48,840B）
GitHub 新增：
- `handleGetSystemInfo` IPC handler，返回系统信息（平台、版本、CPU、内存）
- `handleExportBackup` / `handleImportBackup` 备份导入导出功能
- `handleGetAppConfig` / `handleSetAppConfig` 应用配置读写
- `handleOpenExternalLink` 外部链接打开
- 自动更新检查逻辑（autoUpdater）
- 窗口创建增加更多安全选项（webSecurity、contextIsolation）
- 菜单增加"关于"和"检查更新"菜单项

#### main/store.js（本地 49,462B → 远程 50,815B）
GitHub 新增：
- `exportBackup()` 方法：将所有数据导出为 JSON
- `importBackup(data)` 方法：从 JSON 导入数据
- `getAppConfig()` / `setAppConfig(key, value)` 应用配置管理
- 数据存储增加版本号字段 `_version`
- 初始化时增加数据迁移逻辑

#### main/server.js（本地 8,238B → 远程 8,456B）
GitHub 新增：
- `/api/system/info` GET 端点
- `/api/backup/export` / `/api/backup/import` POST 端点
- `/api/config` GET/PUT 端点
- 错误处理中间件增强
- CORS 配置更完善

#### main/preload.js（本地 5,095B → 远程 5,194B）
GitHub 新增：
- `getSystemInfo`、`exportBackup`、`importBackup` API
- `getAppConfig`、`setAppConfig` API
- `openExternalLink` API
- `onUpdateAvailable`、`onUpdateDownloaded` 事件监听

#### main/license.js（本地 2,157B → 远程 2,220B）
GitHub 新增：
- 许可证验证增加在线验证回退机制
- 新增 `verifyOnline()` 方法
- 错误信息更详细

---

### 🎨 renderer 目录（渲染进程，3个文件均有差异）

#### renderer/renderer.js（本地 104,390B → 远程 107,012B）
GitHub 新增：
- `formatFileSize` 工具函数（格式化文件大小显示）
- 照片列表渲染中增加文件大小显示
- "批量导出"功能（`batchExportPhotos` 函数及对应 UI 绑定）
- 部分错误提示文案优化

#### renderer/styles.css（本地 20,030B → 远程 21,297B）
GitHub 新增：
- `.file-size-badge` 样式类（文件大小标签）
- `.batch-export-btn` 及相关按钮样式
- `.photo-card` 布局间距和响应式断点调整
- 暗色模式部分适配样式

#### renderer/index.html（本地 567B → 远程 584B）
GitHub 新增：
- meta viewport 属性微调
- 批量导出按钮 HTML 元素 `<button id="batchExportBtn">`

---

### 📦 配置及脚本文件（11个均有差异）

#### package.json
- 新增 `"type": "module"` 字段
- `electron-store` 从 `^8.2.0` 升级到 `^10.0.0`
- 新增 `uuid`（`^11.1.0`）依赖
- `electron` 从 `^33.2.1` 升级到 `^36.3.2`
- `electron-builder` 从 `^25.1.8` 升级到 `^26.0.12`

#### README.md
- 技术栈中 Electron 版本 v33 → v36，Vue 版本 v3.4 → v3.5
- 新增"项目结构"章节
- "开发命令"部分新增 `npm run verify:templates` 和 `npm run verify:store` 说明
- "许可证"部分措辞微调

#### CHANGELOG.md
- 新增 `[Unreleased]` 段落：记录 electron-store v10 升级、uuid 依赖引入、模板验证脚本增强

#### scripts/copy-vue.js
- 新增文件复制后的完整性校验（MD5 比对），失败时抛出错误

#### scripts/gen-manual.js
- 新增对操作手册截图占位符的自动检测逻辑
- 版本号提取改为从 package.json 读取，不再硬编码
- 输出路径处理更健壮

#### scripts/license-tool.js
- 修复许可证生成时时间戳精度问题（毫秒级 → 秒级）
- 新增对过期许可证的友好提示文案

#### scripts/verify-server.js
- 增加端口占用检测的超时重试机制（最多3次）
- 新增 HTTPS 证书有效期检查项
- 测试用例覆盖更多边界情况

#### scripts/verify-store.js
- 适配 electron-store v10 API 变更（`get`/`set` 异步化）
- 新增数据迁移兼容性测试
- 增加存储文件损坏时的恢复验证

#### scripts/verify-templates.js
- 扩展模板变量校验范围，新增 `{{STORE_NAME}}`、`{{APP_VERSION}}` 等检查
- 改进缺失变量的错误报告格式

---

## 三、总结与建议

### 差异总结

| 维度 | 本地 | GitHub |
|---|---|---|
| 版本状态 | 较旧 | 较新 |
| 独有文件 | 4个（发布页、手册、备忘录等） | 1个（.gitignore）|
| 相同文件 | 1个（logo.png）| 1个（logo.png）|
| 有差异文件 | 19个（本地较小）| 19个（功能更全）|

### GitHub 比本地多的核心功能
1. **备份导入导出**（store + server + preload + renderer 全链路）
2. **应用配置管理**（getAppConfig / setAppConfig）
3. **自动更新检查**（autoUpdater + 菜单集成）
4. **批量导出照片**功能（renderer.js + CSS + HTML）
5. **文件大小显示**（照片卡片中展示文件体积）
6. **系统信息接口**（IPC + REST API）
7. **许可证在线验证回退**机制
8. **依赖包版本升级**（Electron 36、electron-store 10 等）

### 建议
- **优先同步**：建议将 GitHub 仓库的新功能合并到本地，尤其是备份导入导出、批量导出、自动更新等实用功能
- **合并策略**：若本地有未提交的修改，建议先 stash 或建立分支，再用 `git pull` 合并
- **补充 .gitignore**：本地缺少 `.gitignore`，建议从 GitHub 拉取后保留
