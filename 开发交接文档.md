# 星期衣精致洗衣衣物照片系统 · 开发交接文档

> 适用版本 v1.1.10 · 文档日期 2026-09-24 · 项目路径 E:\软件开发\星期衣精致洗衣衣物照片系统

## 一、项目速览

- 定位：洗衣门店「衣物拍照存档」桌面系统 —— 扫码建档、最大分辨率拍照、拍摄时间水印烧录、多机联网协同、照片查询与导出。
- 技术栈：Electron 33 + Vue 3（渲染层为单文件应用），运行时零第三方依赖；打包 NSIS（Windows x64）。
- 仓库与发布：https://github.com/Jace-Hao/xingqiyi-laundry-photo —— 对外发布走 GitHub Releases；下载页为 docs/index.html（发布页）。
- 当前版本：v1.1.10（2026-09-24）。本机 dist 目录仅保留 1.1.8 ~ 1.1.10 安装包（09-24 整理后）；历史版本见 GitHub Releases（v0.1.8 起）。
- 质量基线：npm run verify 共 9 个脚本、384 项断言全绿；界面截图流水线与行为测试见第七章。

## 二、快速上手

| 命令 | 说明 |
| --- | --- |
| `npm install` | 首次安装依赖（postinstall 会自动复制 Vue 运行时到 renderer/assets） |
| `npm start` | 以开发模式启动应用 |
| `npm run verify` | 全量无头校验（提交前必跑，当前 384 项） |
| `npm run build` | 打包 Windows 安装包到 dist\（网络受限时见第六章镜像说明） |
| `npm run sync-manual` | 同步操作手册（build / verify 会自动执行，无需手动跑） |

## 三、目录结构

| 目录 | 说明 |
| --- | --- |
| main/ | 主进程与数据层：main.js（入口与 IPC）、store.js（数据与业务）、server.js（局域网 HTTP 服务）、preload.js（渲染层桥）、credentials.js（凭据）、thumb.js（缩略图）、license.js（激活）、update-download.js（更新下载核心：多地址自动降级） |
| renderer/ | 界面层：renderer.js（Vue 单文件应用）、styles.css（「墨绿 × 黄铜 × 纸白」设计体系）、assets/（logo、内置手册、Vue 运行时） |
| scripts/ | 校验与构建辅助：9 个 verify / sync 脚本、copy-vue.js |
| docs/ | 发布页（index.html）、操作手册源文件（manual.md）、预览图与 logo；activate.html 为私有激活页（不入库） |
| dist/ | 打包产物（当前保留 1.1.3 / 1.1.4 安装包 + win-unpacked + 构建缓存） |
| 开发工具/ | 本机开发工具（不入库）：截图与自检、行为测试、发布与补丁脚本 —— 见第七章 |
| web/ | 网页版适配层（未定稿，不入库） |
| license-tools/、license-tool-gui/ | 激活码工具（私有，严禁入库 / 外发） |
| node_modules/ | 依赖（不入库） |

本机其他相关位置（非仓库）：

- `E:\autoclaw\deliverables\` —— 项目总览页 + 本交接文档的 HTML 副本；
- 软件安装目录下的「软件更新」文件夹 —— 更新安装包统一存放处（见第四章）；
- 回收站 —— 2026-09-22 整理移入的旧安装包与过期文件（可恢复，见第十章）。

根文件：README.md（使用说明）、CHANGELOG.md（版本日志）、待修复及增加功能.md（需求台账）、本交接文档。

## 四、核心机制（维护向）

双模式运行、数据位置、拍照水印、缩略图缓存、离线补传、四级权限等基础机制详见 README 与项目总览页；以下是维护者最需要知道的重点。

### 更新链路（v1.1.4 起）

1. 检查更新：api.github.com 直连失败/超时后，自动经 `gh-proxy.com` 镜像重试（整体 24 秒超时兜底）。
2. 下载安装包（main/update-download.js）：候选链为「直连 → 三个国内加速通道（gh-proxy.com / ghproxy.net / gh.ddlc.top）」。连接失败、15 秒断流、首段速度过慢均自动切换下一通道；下载进度会标注当前通道（直连 / 国内加速），完成提示注明是否走了加速；取消立即生效且不会继续切换通道。
3. 备用与内网分发：统一使用软件安装目录下的「软件更新」文件夹（历史位置：系统下载目录、%APPDATA%\updates，应用启动时会自动搬迁）；启动与「检查更新」会扫描该文件夹并提示。
4. 强制推送：服务端管理员选包开启后，客户端下次登录静默下载（内网地址直连下载，不走加速）。
5. 维护：加速镜像失效时改 main/main.js 顶部的 `GH_ACCEL` 列表即可；接口加速前缀为 `GH_ACCEL_API`（当前仅 gh-proxy.com 支持代理 api.github.com）。

### 安装更新自保护（v1.1.6 起）

- 问题背景：安装包存放在 安装目录\软件更新\ 内，electron-builder 默认的「关闭正在运行的应用」按 $INSTDIR 路径前缀匹配进程，会把安装器自己一并匹配——温和阶段发 WM_CLOSE 直接关掉向导，强制阶段直接结束自身进程；且旧版（≤v1.1.5）卸载器同款逻辑会在升级时把新安装器一起杀掉（旧文件被卸、新文件未装）。
- 修复①（build/installer.nsh，electron-builder 自动拾取）：`customCheckAppRunning` 改为按进程名精确 tasklist/taskkill（只针对应用，不碰安装器/卸载器）；温和关闭失败再逐级 `/F /T` 强杀；仍失败弹重试对话框。
- 修复②（同文件 `customInit`）：从「…\软件更新」目录内启动安装器时，先复制自身到 %TEMP%（保留原文件名）再从那里重启，脱离旧卸载器的路径匹配范围。
- 修复③（main/main.js）：关闭事件检测到「{包名}-setup*」或「Uninstall {产品名}*」进程时真正退出（不再隐藏托盘）；「立即安装」启动安装器后轮询监视，检测到安装器进程即自动退出让位（startUpdaterCoExitWatcher）。
- 验证：隔离测试副本（改名构建）跑「旧版运行中 + 升级安装」与「新版运行中 + 重装」两场景，全流程到完成页、无孤儿临时目录、无自杀。
### 查询页分页与当日订单表格（v1.1.7 ~ v1.1.10）

- 查询页分页器：上一页/下一页之间有页码下拉（goPage + .pager-select），选项「当前页/总页数」，越界自动收敛；仅 QueryPage，管理端日志页分页器未动。
- 「⬇ 下载当日订单」按钮（筛选栏「重置」旁，任何筛选状态可见——批量栏在列表为空时会隐藏）：一键导出今天录入的全部订单，**只生成表格不复制照片**（v1.1.9 原为完整导出，v1.1.10 按反馈改为仅表格，供洗衣管家上传助手同步照片使用）。
- 实现：exportPhotosByDate 带 tableOnly 参数——CSV 列 = 条码 / 已存档张数 / 照片在本系统的实际存放文件夹（服务端按 photoDir 实路径；客户端写服务端相对目录）；旧版服务端无此模式时渲染层自动回退完整导出；确认框与 toast 明确「不含照片」。
- 测试：exporttoday-test.js（stub 覆写 confirm 与选目录，断言按钮存在 / 确认框含今日日期 / 成功 toast / 可重复触发）；截图识图确认上屏排版。

### 输入法组合态防护（v1.1.8 起）

- 所有 window 级 keydown 处理器（拍照页 onKeydown、查询页灯箱 onKeydown）开头必须放行输入法组合态：`if (e.isComposing || e.keyCode === 229) return;`。
- 否则中文输入法按回车确认候选词会被 preventDefault 强杀组合并失焦，表现为「输入内容删除后无法再次输入」。
- 新增任何键盘快捷键时照抄这一行；测试见 capture-input-test.js 场景 D（keyCode 229 模拟，修复前 FAIL / 修复后 PASS）。

### 摄像头按需占用（v1.1.3 起）

- 拍照页空闲 3 分钟自动休眠并释放摄像头；窗口最小化/隐藏立即释放；回到前台自动恢复；
- 唤醒方式：扫码、按空格、点击「唤醒拍摄」按钮；另有「释放摄像头」手动按钮；
- 实现位于 renderer.js 的 CapturePage 组件（常量 CAM_IDLE_DEFAULT_MS 可调，测试可用 window.__xqyCamIdleMs 覆盖）。

### 图标与快捷方式

- 安装包与桌面快捷方式图标同源：win.icon = renderer/assets/logo.png（500×500，源文件 logo.svg）；
- 换图标后如系统仍显示旧图标：重启「Windows 资源管理器」或运行 `ie4uinit.exe -show`；任务栏钉住的需取消后重新钉。

## 五、版本历史

| 版本 | 日期 | 主题 |
| --- | --- | --- |
| v1.1.10 | 2026-09-24 | 「下载当日订单」改为仅生成表格（不复制照片，供上传助手同步） |
| v1.1.9 | 2026-09-24 | 查询页新增「下载当日订单」一键导出按钮 |
| v1.1.8 | 2026-09-24 | 修复输入法打字被快捷键打断（删除内容后无法再次输入） |
| v1.1.7 | 2026-09-24 | 查询页分页器新增页码下拉选择 |
| v1.1.6 | 2026-09-24 | 修复安装更新包「双杀 / 无法关闭」：安装器按进程名精准关闭 + 自复制 TEMP 运行 + 软件侧协同退出（新增 build/installer.nsh） |
| v1.1.5 | 2026-09-23 | 打开窗口默认最大化修复 |
| v1.1.4 | 2026-09-22 | 更新下载自动加速（国内镜像多路降级 + 接口加速兜底） |
| v1.1.3 | 2026-09-22 | 拍照页摄像头按需占用（空闲释放 / 最小化释放 / 多路唤醒） |
| v1.1.2 | 2026-09-22 | 软件图标更换（衬衫+相机）＋ 更新文件夹统一到安装目录「软件更新」 |
| v1.1.1 | 2026-09-22 | 界面品牌化精修（墨绿 × 黄铜 × 纸白）+ 线性图标体系 |
| v1.1.0 | 2026-09-20 起 | 门店分组、四级角色、时间水印、导出汇总、唯一登录、保留期、缩略图缓存（多批） |
| v1.0.0 | 2026-09-13 | 更换激活密钥并整改授权机制泄露风险 |
| v0.1.9 | 2026-09-12 | 完成所有待修复及增加功能（台账清空） |
| v0.1.7 ~ v0.1.8 | 2026-09-08 | 激活码体系、离线冗余、导出、强制推送、内置手册等 |
| v0.1.0 ~ v0.1.6 | 2026-09-02 ~ 09-04 | 立项初版至早期迭代 |

详细逐版本变更见 CHANGELOG.md；发布资产见 GitHub Releases。

## 六、构建与发布流程

1. 升版本号：package.json → README（3 处）→ docs/manual.md（4 处）→ CHANGELOG（新段）→ renderer.js 中的示例文件名；发布页版本串在第 3 步统一改。
2. 构建（可能遇到 GitHub 网络抖动，需下载 Electron 运行时）。推荐带镜像：
```
$env:ELECTRON_MIRROR = 'https://npmmirror.com/mirrors/electron/'
$env:ELECTRON_BUILDER_BINARIES_MIRROR = 'https://npmmirror.com/mirrors/electron-builder-binaries/'
npm run build
```
3. 发布页同步：以 `开发工具\发布与补丁\patch-docs-*.js` 最新版本号为模板（现有 116~119+110，升版时滚动复制一份改参数）（从构建产物实读大小/SHA 回填，带命中数断言与残留检查）。
4. 提交与推送：git add 指定文件 → commit → push（GitHub 直连不稳，建议脚本化重试）→ 打 tag 并推送。
5. 发布 Release：以 `开发工具\发布与补丁\github-release-*.ps1` 最新版本号为模板（现有 116~119+110；改顶部 tag / 文件名即可）。脚本自动：组装发布说明（CHANGELOG 段 + SHA256）→ 建草稿 → 上传 blockmap + exe → 转正式 → 带凭据与匿名双路复核。
6. 终检：匿名访问 releases/latest 应指向新版本；直链与三个加速通道抽查（HTTP 200/206）。

注意：不要修改已发布版本的资产；一切新内容走「版本号升级 + 新 Release」。

## 七、验证与开发工具

- 无头校验：`npm run verify` —— 9 个脚本、384 项断言（store 105 / server 35 / templates 12 / watermark 48 / thumb 31 / credentials 61 / manual-sync 17 / manual-render 75）。
- 界面截图流水线（`开发工具\截图与自检\`）：
  - capture.js：真实渲染层 + 假桥接 + 假摄像头，截取 8 个页面（登录 / 总览 / 数据 / 首页 / 拍照 / 查询 / 设置 / 手册）到 out\；
  - page-check.js：发布页自检（版本串、下载按钮、预览图断言 + 桌面与移动截图）；
  - admin-check.js：发布页「管理员工具」入口自检；
  - 运行：`node_modules\electron\dist\electron.exe 开发工具\截图与自检\capture.js`（其余同理）。
- 行为测试（`开发工具\测试\`）：
  - cam-test.js：摄像头按需占用 8 项（取流 / 不休眠 / 空闲释放 / 空格唤醒 / 隐藏释放 / 前台恢复 / 手动释放 / 离开页面释放）；
  - dl-test.js：下载自动降级 11 项（候选生成 / 正常下载 / 拒连切换 / 卡死切换 / 龟速切换 / 500 切换 / 取消 / 全失败提示 / 进度）；
  - pager-test.js：查询页页码选择 12 项（下拉存在 / 选项数=总页数 / 选页跳转 / 网格联动 / 首末页禁用态）；注意页面有多个 .pager-info（批量栏也用），断言选择器需限定 .pager 容器；
  - capture-input-test.js：输入框行为 20 项（条码框输入/回车/删除/再输入、休眠浮层不遮挡、查询关键词框、输入法组合态回车防护场景 D）；
  - exporttoday-test.js：下载当日订单 3 项（按钮存在 / confirm 含今日日期与「不含照片」提示 / 表格生成 toast；stub 自动选目录并覆写 confirm）。
  - 运行同上（换文件名即可），退出码 0 = 全过；日志写同目录。
改键盘/输入相关 → capture-input-test 复跑；改导出 → exporttoday-test 复跑；改拍照页 → cam-test 复跑；任何提交前 → npm run verify。

## 八、私有不入库清单（严禁提交 / 外发）

以下文件（.gitignore 已配置）禁止进入公开仓库或对外发送：源代码加密方案.md、license-tools/、license-tool-gui/、scripts/license-tool.js、main/license-secret.json、docs/activate.html、web/、开发工具/、credentials.json。

原因：激活算法与密钥一旦泄露，等于授权体系被绕过；上传前务必 `git status` 逐一确认。

## 九、本机环境与注意事项（踩坑清单）

- 换行符：部分文件在历史 rebase 后为 CRLF —— 编写补丁脚本要用「行尾自适应」（按文件实际 CRLF/LF 匹配与回写），否则会命中失败或产生整文件 diff。
- 临时区易失：`.openclaw\tmp`（E:\软件开发 与 E:\autoclaw 两处）会被外部清理（2026-09-22 已发生）——重要产物及时落进项目内或提交入库，不要长期放临时区。
- GitHub 网络：push、构建、发布都可能需要重试；构建用 npmmirror 镜像最稳；git 凭据经 git credential fill 读取（不落盘）。
- 更新文件夹：位于软件安装目录下「软件更新」，卸载软件会连同清理——需要长期留存的安装包请另存。
- 安装器自我追杀陷阱：安装包放在安装目录内 + electron-builder 默认关闭逻辑按路径前缀匹配 → 升级时新安装器/旧卸载器会杀掉自己或对方（v1.1.6 已修，详见 §四「安装更新自保护」）；以后若改动更新目录名或关闭逻辑，务必重跑隔离升级验证。
- 端口：服务端默认 17521，被占用时在设置中修改。
- 本机可能同时承担门店日常使用（有常驻业务软件）——涉及系统级操作（重启资源管理器、安装/覆盖软件）请避开营业时段。
- 输入法组合态：所有 keydown 快捷键必须带 `isComposing || keyCode===229` 防护（v1.1.8 教训：中文输入法回车确认候选被拦截 → 删除内容后无法再次输入）；合成事件测试全过 ≠ 真实键盘没问题，输入类报障优先查 IME 路径。
- 测试窗口弹在门店机上会被人顺手点击（2026-09-24 实际发生，导致向导被人为推进）：自动化向导测试要把按钮点击做全（「安装(&I)」这类文本会误中单选按钮文案），测试尽量短、即测即清。
- 文档习惯：中文、全角标点；提交信息沿用 `feat / fix / chore / release: ...` 前缀风格。

## 十、整理记录

### 2026-09-24（v1.1.6 ~ v1.1.10 五连发后）

- dist 旧安装包入回收站：1.1.3 ~ 1.1.7 共 5 个版本（exe+blockmap 10 个文件，约 385 MB）——保留 1.1.8 / 1.1.9 / 1.1.10；更早版本均已在 GitHub Releases（v1.1.0 起曾发布）。
- 当日发布五个版本：v1.1.6 安装更新自保护（双杀/无法关闭）/ v1.1.7 查询页页码选择 / v1.1.8 输入法组合态防护 / v1.1.9 当日订单按钮 / v1.1.10 仅表格化；发布脚本逐版本滚动：github-release-116~119+110.ps1、patch-bump/docs-116~119+110.js（开发工具\发布与补丁\，不入库）。
- 新增测试归档：pager-test / capture-input-test / exporttoday-test（开发工具\测试\）。
- 本交接文档全面更新至 v1.1.10 并重生成 HTML（副本同步 E:\autoclaw\deliverables\）。

### 2026-09-22
已移入回收站（可恢复）：

- dist 旧安装包：v0.1.0 ~ v1.1.2 共 12 个版本（24 个文件，约 918 MB）——现仅保留 1.1.3、1.1.4；如需更早版本（0.1.0 ~ 0.1.7 未发布到 GitHub），可在回收站恢复；
- 临时区 7 个目录（xqy-shots / xqy-apply / xqy-page / xqy-ui ×2 / xqy-backup / xqy-dist-backup）与 dirs.txt（约 180 MB）；
- 过期文件：操作手册 v0.1.8.docx、两个旧压缩包（交付包 / 源码包）、废弃脚本 scripts\tmp-create-release.ps1（已从 git 移除）。

已归档进项目「开发工具\」（不入库）：截图流水线全套（含 mock 素材）、重建的 cam-test.js 与 dl-test.js、发布 / 补丁脚本模板、工具说明 README。

结果：dist 体积 1341.5 MB → 422.4 MB；E:\autoclaw\.openclaw\tmp 已清空；全量校验 384 项全绿；三项工具在新位置冒烟全过（下载降级 11/11、摄像头 8/8、截图 8 页）。

备注：`E:\autoclaw\deliverables\xqy-xygj-bridge` 为早期「照片同步桥接」实验代码（未启用、未纳入本项目），保持原位未动。

## 十一、交接清单（接手必读）

1. 读完 README.md 与本交接文档，浏览 CHANGELOG.md 与「待修复及增加功能.md」（需求台账）。
2. `npm install && npm start` 把应用跑起来，管理端与客户端各过一遍主流程。
3. 跑 `npm run verify` 确认基线全绿；再各跑一次 cam-test / dl-test 熟悉工具。
4. 出下一版：按第六章流程走（脚本模板都在 开发工具\发布与补丁\）。
5. 改动私密文件（激活相关）前先读第八章；任何提交前 `git status` + verify 双检查。
6. 现场配置以客户现场实际为准（开发机与门店机可能分开部署）。
7. v1.1.10 的当日订单表格（仅表格不复制照片）为 09-24 新特性：留意门店实际使用反馈（表格列是否够用、与洗衣管家上传助手的衔接是否顺畅）。
