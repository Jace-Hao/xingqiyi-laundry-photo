# 源代码加密方案

## 概述

本文档描述了星期衣精致洗衣衣物照片系统的源代码加密方案，用于保护核心业务逻辑不被轻易反编译。

## 加密范围

### 需要加密的文件
- `main/main.js` - 主进程核心逻辑
- `main/store.js` - 数据存储层
- `main/server.js` - HTTP API 服务
- `main/license.js` - 激活码算法
- `main/preload.js` - 预加载脚本
- `renderer/renderer.js` - 渲染进程主逻辑

### 不加密的文件
- `package.json` - 依赖配置
- `README.md` - 项目说明
- `renderer/index.html` - HTML入口
- `renderer/styles.css` - 样式文件
- `renderer/assets/*` - 静态资源

## 加密方案

### 方案一：JavaScript Obfuscator（推荐）

使用 `javascript-obfuscator` 工具对JavaScript代码进行混淆。

#### 安装依赖
```bash
npm install --save-dev javascript-obfuscator
```

#### 混淆配置
```javascript
// obfuscator-config.js
module.exports = {
  compact: true,
  controlFlowFlattening: true,
  controlFlowFlatteningThreshold: 0.75,
  deadCodeInjection: true,
  deadCodeInjectionThreshold: 0.4,
  debugProtection: false,
  disableConsoleOutput: false,
  identifierNamesGenerator: 'hexadecimal',
  log: false,
  numbersToExpressions: true,
  renameGlobals: false,
  selfDefending: false,
  simplify: true,
  splitStrings: true,
  splitStringsChunkLength: 10,
  stringArray: true,
  stringArrayCallsTransform: true,
  stringArrayEncoding: ['base64'],
  stringArrayIndexShift: true,
  stringArrayRotate: true,
  stringArrayShuffle: true,
  stringArrayWrappersCount: 1,
  stringArrayWrappersChainedCalls: true,
  stringArrayWrappersParametersMaxCount: 2,
  stringArrayWrappersType: 'variable',
  stringArrayThreshold: 0.75,
  target: 'node',
  unicodeEscapeSequence: false
};
```

#### 执行混淆
```bash
npx javascript-obfuscator main/main.js --output dist/main/main.js --config obfuscator-config.js
npx javascript-obfuscator main/store.js --output dist/main/store.js --config obfuscator-config.js
npx javascript-obfuscator main/server.js --output dist/main/server.js --config obfuscator-config.js
npx javascript-obfuscator main/license.js --output dist/main/license.js --config obfuscator-config.js
npx javascript-obfuscator main/preload.js --output dist/main/preload.js --config obfuscator-config.js
npx javascript-obfuscator renderer/renderer.js --output dist/renderer/renderer.js --config obfuscator-config.js
```

### 方案二：asar 加密打包

Electron 默认使用 asar 打包应用，可以额外使用加密工具对 asar 包进行加密。

#### 安装依赖
```bash
npm install --save-dev asar
```

#### 打包并加密
```bash
# 先打包为 asar
npx asar pack . app.asar

# 使用第三方工具加密（如 asar-encrypt）
# 注意：这需要额外的加密工具支持
```

### 方案三：Native 模块保护

将核心算法用 C++ 编写为 Node.js Native 模块（.node 文件），编译后的二进制文件更难反编译。

#### 示例：激活码算法 Native 模块

```cpp
// license_native.cc
#include <napi.h>
#include <openssl/hmac.h>
#include <openssl/sha.h>

Napi::String GenActivationCode(const Napi::CallbackInfo& info) {
  Napi::Env env = info.Env();
  std::string machineCode = info[0].As<Napi::String>().Utf8Value();
  
  // 实现激活码生成算法
  unsigned char result[EVP_MAX_MD_SIZE];
  unsigned int result_len;
  HMAC(EVP_sha256(), "secret_key", strlen("secret_key"),
       (unsigned char*)machineCode.c_str(), machine.length(),
       result, &result_len);
  
  // 转换为字符串并返回
  return Napi::String::New(env, activationCode);
}

Napi::Object Init(Napi::Env env, Napi::Object exports) {
  exports.Set("genActivationCode", Napi::Function::New(env, GenActivationCode));
  return exports;
}

NODE_API_MODULE(license_native, Init)
```

## 推荐方案

**推荐使用方案一（JavaScript Obfuscator）**，原因：
1. 实现简单，无需额外编译工具
2. 混淆效果良好，能有效防止直接阅读源码
3. 兼容 Electron 打包流程
4. 性能损失可控

## 加密后部署

### 打包流程
1. 执行代码混淆
2. 将混淆后的文件复制到 `dist` 目录
3. 使用 electron-builder 打包为安装程序
4. 发布到 GitHub Releases

### 注意事项
- 混淆后的代码体积会增大 2-3 倍
- 运行性能会有 10-20% 的损失
- 建议在发布前充分测试混淆后的代码
- 保留原始源码用于开发维护

## 安全级别评估

| 方案 | 安全级别 | 实现难度 | 性能影响 |
|------|---------|---------|---------|
| JavaScript Obfuscator | 中等 | 简单 | 低 |
| asar 加密 | 低 | 简单 | 无 |
| Native 模块 | 高 | 复杂 | 无 |

## 更新日志

- 2026-09-12: 初始版本，记录加密方案
