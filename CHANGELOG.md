# 更新日志

## v1.1.6（2026-09-24）

### 修复

- **安装更新包不再「双杀」/卡「无法关闭」**：修复升级安装时「软件关闭的同时安装程序也被关闭」「反复提示无法关闭导致安装中断」的问题。根源：安装包存放在安装目录的「软件更新」文件夹内，安装器自带的关闭逻辑按路径前缀匹配进程，会把安装器自己一并匹配并结束；且软件在服务端模式下「点关闭 = 隐藏到托盘」，安装器实际关不掉，只能反复强杀。本版本双侧修复：
  - 安装器改为按进程名精确关闭（只针对本应用，绝不再误伤安装器自身）；从「软件更新」文件夹内启动安装时，安装器自动改从系统临时目录运行，彻底避开旧版卸载器的同类误判（否则升级时旧卸载器会把新安装器一起杀掉，旧文件被卸、新文件未装）；
  - 软件侧配合：检测到更新安装器/卸载器运行时，点关闭会真正退出（不再隐藏托盘）；在软件内点「立即安装」后，软件检测到安装器启动会自动退出让位，升级全程无需手动关闭软件。

### 说明

- 覆盖安装即可，数据与使用方式不变；从 v1.1.0～v1.1.5 升级到本版本即可修复上述问题。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.6.exe`（Windows x64，NSIS 安装向导）。

## v1.1.5（2026-09-23）

### 修复

- **打开窗口默认最大化**：修复部分场景下打开软件窗口未最大化的问题——此前「开机自启静默驻留后从托盘/快捷方式唤出」「上次手动还原过窗口」等情况会以小窗打开；现在启动与唤出一律自动最大化显示。

### 说明

- 覆盖安装即可，数据与使用方式不变。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.5.exe`（Windows x64，NSIS 安装向导）。

## v1.1.4（2026-09-22）

### 新增

- **更新下载自动加速**：GitHub 直连不稳定时自动降级——下载安装包时会自动切换到国内加速通道重试（连接失败、中途断流、速度过慢均自动切换，内置多个加速镜像）；「检查更新」接口同样支持加速兜底，网络受限时也能正常「一键下载更新」。下载进度会标注当前通道（直连 / 国内加速），完成提示会注明是否走了加速通道。

### 说明

- 覆盖安装即可，数据与使用方式不变。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.4.exe`（Windows x64，NSIS 安装向导）。

## v1.1.3（2026-09-22）

### 修复

- **拍照页摄像头占用优化**：停留拍照页不退出时摄像头不再长期占用——空闲 3 分钟自动休眠并释放设备，窗口最小化/隐藏立即释放，回到前台自动恢复画面；扫码、按空格或点击「唤醒拍摄」即可继续拍摄。另新增「释放摄像头」手动按钮，拍完即可一键释放。

### 说明

- 覆盖安装即可，数据与使用方式不变。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.3.exe`（Windows x64，NSIS 安装向导）。

## v1.1.2（2026-09-22）

### 变更

- **软件图标更换**：全新「衬衫 + 相机」图标，更直白体现「衣物拍照存档」定位；软件界面、任务栏、系统托盘、安装包与桌面快捷方式同步换新。
- **更新文件夹统一**：更新下载、备用更新与强制推送三类场景统一到软件安装目录下的「软件更新」文件夹；旧位置（系统下载目录、用户数据目录）中的历史安装包会在启动时自动搬迁，安装目录不可写时自动回退用户数据目录。

### 说明

- 覆盖安装即可，数据与使用方式不变。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.2.exe`（Windows x64，NSIS 安装向导）。

## v1.1.1（2026-09-22）

### 变更

- **界面品牌化精修**：全界面从通用蓝色主题升级为品牌「墨绿 × 黄铜 × 纸白」设计体系，与产品发布页、品牌形象统一。
  - 全量重建设计令牌：品牌绿阶、中性色、深绿侧栏、金色点缀；统一阴影、圆角与字体层级（数字使用等宽数字）。
  - 组件全面打磨（40 余处）：按钮 / 输入框 / 卡片 / 表格 / 标签 / 分页 / 弹窗 / 提示条 / 灯箱 / 拍照页 / 记录网格 / 空态 / 批量操作栏等。
  - 侧边栏导航图标由 emoji 升级为统一的线性 SVG 图标系统。
  - 登录页与首次启动设置页更换为品牌深绿渐变背景。
  - 新增按钮按下反馈、键盘焦点环（focus-visible）、跟随系统「减少动态效果」偏好（prefers-reduced-motion）与统一滚动条样式。

### 说明

- 本版本为界面视觉升级，功能与数据流程与 v1.1.0 一致，覆盖安装即可。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.1.exe`（Windows x64，NSIS 安装向导）。

## v1.1.0（2026-09-20）

### 新增

- **门店分组**：账号可归属门店，同一门店的账号可互相查询彼此的订单照片。门店由系统管理员在「用户与权限管理」中分配，客户端在「设置」页只读查看自己的所属门店。查询页与照片详情会标注订单归属门店；管理端可按门店筛选订单，关键词搜索也能直接搜门店名。
- **四级角色权限体系**：账号角色由原来的「管理员 / 客户端」两分，改为四级并各自固定权限：
  - **系统管理员**：查看全部与全部门店订单、设置账号与权限、查看所有门店操作日志、修改系统设置。
  - **门店管理员**：查看本门店全部订单与本门店操作日志；只查不拍、不能删订单、不能管理账号与系统设置。
  - **拍照账号**：拍照录入订单 + 查询本人与同门店订单。
  - **查询账号**：仅查询本人与同门店订单，不能拍照。
- **照片拍照时间水印**：拍照时在照片右上角烧录「年-月-日 时:分」水印。水印写入照片本身而非界面叠加，因此导出、打印后的照片同样带有收衣时间，可作为存证；字号随图像分辨率自动缩放，高分辨率照片下依然清晰可读。
- **导出表格按订单汇总**：按日期导出的归档表格改为「每个条码一条记录」，包含条码、照片数量与归档文件夹位置，不再逐张照片罗列；照片本身仍全部导出到对应条码文件夹。
- **账号角色构成总览**：管理端「数据总览」页新增四级角色的账号数量统计。
- **服务端开机自动启动**：系统设置新增「开机自动启动」开关（仅服务端模式可开启）。开启后登录 Windows 时自动启动并静默驻留托盘、不弹窗，持续为各客户端提供照片服务，避免门店断电重启后服务没起来。软件改为**单实例**运行：自启后再次双击图标不会启动第二个程序，而是唤起已在运行的窗口，避免两个进程争抢同一服务端口。本机从服务端切回客户端时会自动取消该设置，防止工位机变成开机常驻。
- **登录框记住账号与密码**：登录页新增账号下拉，可选择本机保存过的账号（显示各自角色与门店，带 🔑 表示已存密码）。密码经 Electron `safeStorage` 系统级凭据保护（Windows DPAPI / macOS Keychain）加密后保存，**不明文落盘、不上传服务器**；系统凭据保护不可用时自动禁用「记住密码」，**不会退化为明文保存**。默认只记住账号名，记住密码需显式勾选，界面对共用电脑场景给出风险提示。
- **内置操作手册**：新增侧边栏「📖 操作手册」页（所有角色可见），章节按当前账号角色自动裁剪——拍照账号看不到「用户与权限」「系统设置」等章节，系统管理员可见全部。手册随软件打包，离线环境也可查阅。打包前经 `prebuild` 钩子自动从 `docs/manual.md` 同步，并校验手册版本号与软件版本一致，不一致即中断构建，避免发版时遗漏更新文档。
- **账号唯一登录**：同一账号同一时间只能在一台电脑上登录，异地登录会使原会话立即失效。被顶下线的电脑会明确提示「账号已在其他设备登录」并退回登录页，提示中写明顶下线的登录时间与来源 IP，便于确认是谁在用该账号——不会与普通「未登录」混为一谈，避免店员误判为密码过期而反复重试。**离线登录不受此限制**：服务器失联时客户端用本机镜像登录，不经服务端签发会话，因此不会被顶下线；离线数据在重新登录后仍会补传，即使该账号此时已在别处登录。已失效的会话记录按保留期（7 天）与数量上限（200 条）自动清理，避免会话文件无限膨胀。
- **订单数据保留期（超时自动删除）**：系统管理员可在「系统设置 → 订单数据保留期」设定订单保存期限，超期订单及其照片文件自动清理，用于控制长期累积占用的磁盘空间。因为这是**不可逆删除**，实现时设了多道防误删保护：默认关闭（未配置时任何情况下都不会删数据）、最小 1 天（显式拒绝 0 与负数，因为「保留 0 天」等于清空全部订单）、**全删熔断**（若某次清理将删除全部订单——例如系统时钟异常跳到未来导致所有订单都判定为超期——自动清理拒绝执行并告警，而不是照删）、仅服务端模式执行。界面提供「试算」只统计不删除，让管理员在真删前看到确切影响条数；保存设置不会立即删除；首次开启或缩短保留期时弹确认框提示影响面。每次自动清理记入操作日志（操作类型「自动清理过期订单」），写明清理条数与保留期限。仅系统管理员可设置。

### 修复

- **操作日志筛选类型与实际不符**：日志页的「操作类型」下拉此前由前端硬编码，缺少「新增条码」等多个类型，且含有后端从不产生的「新增存档」，导致部分日志无法筛选。现改为由后端提供单一数据源（登记清单并自动兼容历史日志中实际出现过的类型），前端不再硬编码，后续新增日志类型不会再次脱节。
- **查询页打开时只显示 12 张照片**：单页数量此前在首屏查询时仍用固定初值，页面大小变化不生效。现改为先按可视区域算出每页数量再查询，并在布局稳定后补算一次，窗口大小变化时数量随之增减。
- **无效操作入口**：对无权删除订单的角色（门店管理员、查询账号）不再显示记录勾选框与批量删除按钮，避免点击后必然失败；照片详情中查看同门店同事录入的订单时，给出「仅可查看与导出，不可删除」的说明而非可点的删除按钮。
- **客户端离线降级丢失门店与角色**：远程登录成功后镜像到本机的账号此前未保留门店字段，且角色被压回旧的二值模型，导致服务器失联时同门店互查与角色权限失效。现已完整保留。
- **离线存档可能永久无法补传**（配合唯一登录发现并修复）：客户端的离线会话表按插入顺序保存，而补传逻辑取第一个带服务端令牌的条目。账号在其他设备登录后，本机旧令牌已被服务端顶下线，但旧条目仍残留且排在新条目之前——补传会先拿这个失效令牌请求，服务端返回失败后同步中断，每轮重试都撞同一个死令牌，导致离线录入的数据永远传不上去、待同步队列无限堆积。现做两层修复：登录成功时清理该账号的旧会话条目；补传时若识别到令牌已被顶下线，主动移除该条目并给出「请重新登录后再同步」的明确原因。**离线数据本身不会丢失**——队列只在服务端确认接收后才移除条目。

### 变更

- **权限随角色固定派生**：拍照 / 查询权限不再单独勾选，改由角色决定。这样不会出现「门店管理员可拍照」「查询账号可录入」这类与角色定义矛盾的账号；账号编辑页会以只读方式展示当前角色的实际权限，并在页面上说明各角色能做什么。
- **可见范围与删除权限分离**：同门店互查只扩大「可查看与导出」的范围，删除权限仍限于本人与系统管理员，避免同事之间误删订单。
- **历史记录保留门店快照**：订单记录保存的是录入时所属的门店。账号调店后，其历史订单仍归属原门店（衣物实际收存于原门店），只有此后新增的订单才归属新门店。
- **门店管理员未分配门店时的兜底**：这种异常配置下日志与账号下拉返回空集，而不是放开为「可见全部」。
- **网格缩略图懒加载**：首页、查询页、数据总览的照片网格改为进入可视区域才加载并异步解码，减少一次性加载整页照片的开销；照片预览仍使用原图，不影响查看清晰度。
- **单实例运行（行为变化）**：本软件现在同一时间只运行一个实例。重复启动（双击桌面图标、开机自启后再手动启动）不会打开第二个程序，而是把已在运行的窗口唤到前台。此前多开会争抢同一服务端口，导致后启动者报端口占用、或客户端连到错误实例。
- **同一账号唯一登录（行为变化）**：此前同一账号可在多台电脑同时登录，现在不可。异地登录会顶掉原会话，被顶下线的电脑会退回登录页并提示原因。**升级后若某门店原先多台电脑共用一个账号，将无法同时在线**，需由系统管理员为每台电脑建立独立账号（也便于日志追溯到人）。离线登录不受此限制。
- **从托盘唤出窗口的焦点修复**：静默驻留后首次从托盘打开窗口，此前可能重现 v0.1.7 修过的「窗口显示出来但所有输入框点不动、必须重启软件」问题。现统一走含 `webContents.focus()` 的焦点恢复逻辑。
- **系统管理员可见全部手册章节**：内置手册的章节裁剪对系统管理员一律放开（含店员端操作文档）。此前按标记严格裁剪时，会出现「系统管理员反而比门店管理员看到的章节少」的反直觉结果——因为店员章节的标记未包含系统管理员。
- **新增本地凭据文件**：记住账号/密码功能会在用户数据目录写入 `data/credentials.json`（存加密后的密文与账号元信息）。该文件已加入 `.gitignore`；卸载或手动删除即清除全部保存的登录凭据。

### 安全

- **修复代码加密方案文档被公开（重要）**：`.gitignore` 中该文档的忽略模式写成了「源代码加密说明.md」，与真实文件名「源代码加密方案.md」不匹配，导致这份 v0.1.9 要求「只留本地、不上传仓库」的文档被提交并推送到了公开仓库。现已修正忽略模式并停止跟踪该文件，本地文件保留不受影响。**注意：历史提交中仍可取到该文档全文，彻底清除需重写 Git 历史并强制推送**，是否需要处理请另行决定。
- **记住密码不明文落盘**：保存的密码经 Electron `safeStorage` 系统级凭据保护加密，磁盘上只有密文；界面层的账号列表接口不返回密文，避免密文流入渲染层。系统凭据保护不可用时**只记住账号名、不保存密码**，绝不退化为明文存储。解密失败（系统凭据变更、文件被篡改）时返回空密码并提示手动输入，不使用任何猜测的兜底值。
- **凭据操作不离开本机**：凭据读写接口走本地 IPC，不经客户端→服务端的远程转发通道，因此密码不会被发送到远程服务器。
- **修改系统设置需系统管理员**：开机自启、以及此前的端口/连接码/照片路径/强制推送，统一按四级角色能力矩阵鉴权；门店管理员与店员账号无法修改系统设置。开机自启的开关与取消都会记入操作日志。

### 测试

- 验证套件扩充至 **340 项断言**（`npm run verify`，7 个脚本），本轮新增三个脚本：
  - `verify-credentials.js`（61 项）：凭据存储的安全属性——密码不明文落盘、加密不可用时不降级、解密失败不猜测、列表不返回密文、取消记住会真正清除密文、账号数上限裁剪、损坏文件容错、原子写入等。
  - `verify-manual-render.js`（75 项）：内置手册渲染器——HTML/脚本一律转义（含粗体内嵌标记）、语法子集渲染、角色裁剪与子章节继承、目录锚点唯一、真实手册渲染无残留 markdown。
  - `verify-manual-sync.js`（17 项）：手册同步脚本的校验有效性——版本不一致、不支持的语法、非法角色标记均能被拦截（只测「应当失败」的输入，否则校验形同装饰）。
- `verify-store.js` 由 85 项扩至 105 项：新增开机自启的日志动作登记校验、「主进程调用 `logSystemChange` 时传入的动作类型必须已登记」的跨文件扫描（防止出现「设置成功但日志筛选不到该类型」）；唯一登录用例——同账号不可多地登录、被顶下线提示含「其他设备登录」且不同于普通未登录、顶下线不影响其他账号、已失效会话按保留期与数量上限清理（防止 `sessions.json` 无限膨胀，且不涉及功能正确性、只能靠专项断言发现）、镜像离线会话同样受约束（不能靠旧令牌绕过顶下线）；以及订单保留期的 13 项专项用例，全部针对「会不会误删数据」——默认未配置时不删、非法保留天数（0 / 负数 / 非数字）一律拒绝而非静默兜底、设置后不会立即删数据只给预览数量、`dryRun` 只统计不删除、非系统管理员无权设置或执行、客户端模式不执行、按 `createdAt` 正确划分超期（未超期的保留、超期的连照片一并删除）、重复执行幂等、`createdAt` 缺失的记录不被清理（无法判断年龄则保留）、**自动执行**遇「将删光全部订单」时熔断且不删任何数据、**手动执行**（管理员明确发起）则允许清空全部超期订单——自动与手动的这道区别是刻意设计：熔断只防无人值守的误删，不阻碍管理员的明确操作、取消保留期后恢复禁用、设置与清理均记入操作日志。
- `verify-server.js` 由 28 项扩至 35 项：验证 `revoked` 标志能穿过 HTTP 往返传回界面（客户端模式下靠它强制退回登录页），以及伪造令牌/无令牌不会被误报为顶下线。
- `verify-templates.js` 增强：除模板语法外，还比对模板引用的变量是否都在 `setup` 返回值中注册，避免未注册变量静默渲染为空白（本轮登录页与手册页新增了大量模板绑定）。
- 说明：开机自启依赖 Electron 登录项，会真实读写系统注册表与用户目录，无法在验证脚本中安全覆盖；订单保留期的**自动调度**（启动后延迟执行、周期性检查）同样依赖 Electron 主进程的定时器，未纳入验证脚本。两者的数据层逻辑（读取/设置/清理/熔断）均已覆盖，但调度时机与登录项登记需实机验证。

### 升级说明

- **旧账号自动迁移，无需人工干预**：原「管理员」账号迁移为系统管理员；原「客户端」账号按已有功能开关对应——拍照与查询都开通的迁移为拍照账号，仅开通查询的迁移为查询账号。迁移后各账号实际能力与升级前一致。
- 迁移具备幂等性，重复启动不会改变已迁移的角色；同时会为历史订单回填门店归属（按订单所属账号的当前门店），使升级前的照片也能参与同门店互查。
- 门店管理员为新增角色，需由系统管理员在「用户与权限管理」中新建或调整，并为其分配门店。
- **升级前请确认是否有门店多台电脑共用一个账号**：本版起同一账号唯一登录，共用账号将无法同时在线，需先由系统管理员为每台电脑建立独立账号。此前多地登录同一账号的用法会受影响。
- 本版本安装包：`xingqiyi-laundry-photo-setup-1.1.0.exe`（Windows x64，NSIS 安装向导）。

## v1.0.0（2026-09-13）

### 安全

- **更换激活密钥（重要）**：此前激活算法的密钥被硬编码在随仓库公开的源码中，任何人查看仓库即可自行算出激活码，激活与 7 天试用限制形同虚设。本版起密钥改由运行时从不入库的独立文件读取，公开源码中不再包含任何密钥；已更换为新的随机密钥。
- **激活工具移出公开仓库**：网页版与命令行版激活码生成工具不再随仓库发布，改为仅由项目维护者私下提供给管理员使用；产品发布页相应改为「需向管理员索取」的说明，不再提供工具直达链接。
- **密钥缺失不放行**：若安装包缺失密钥文件，登录会被明确拦截并提示「激活组件不完整」，而不是静默放行，避免「删掉密钥文件即可免费使用」的绕过路径。
- **激活工具去除内嵌密钥**：网页版激活工具改为由本机后端计算激活码、页面仅取回结果。这样即使该页面被远程开放使用，泄露的也只是「输入机器码→得到激活码」的接口本身，密钥不会外泄；并支持设置访问口令。

### 破坏性变更

- **旧激活码与旧机器码全部失效**：机器码由密钥派生，更换密钥后同一台电脑显示出的机器码会发生变化。v1.0.0 之前的激活码无法在新版本使用，需要客户在新版本中重新获取机器码，由管理员重新生成激活码。
- 已激活的电脑升级到 v1.0.0 后会回到未激活状态（按试用/待激活处理），属预期行为。

### 说明

- 本版本安装包：`xingqiyi-laundry-photo-setup-1.0.0.exe`（Windows x64，NSIS 安装向导）。
- 版本号由 0.1.x 提升为 1.0.0，标志激活授权机制修复后达到可正式交付状态。

## v0.1.9（2026-09-12）

### 新增

- **产品发布页**：新增面向用户的产品发布页（`docs/index.html`），左上角使用软件 logo，内置「激活工具」入口链接；页面自动读取 GitHub 仓库最新版本号并显示在下载选项上，发版后无需手动改页面。
- **Web 激活页面**：新增可在浏览器中直接使用的激活码生成页面（`docs/activate.html`），管理员输入客户机器码即可算出激活码，无需在本地运行命令行工具。
- **操作手册 Markdown 化**：操作手册新增 Markdown 版本（`docs/manual.md`），便于在 GitHub 上直接阅读与后续维护，与 Word 版内容保持一致。
- **保持登录状态**：登录成功后记住最后一次登录的账号，下次启动自动填充用户名，店员无需每次重新输入。

### 修复

- **软件打开时为小屏**：窗口现在记住上次的位置与尺寸，首次启动或状态异常时强制最大化显示，不再出现小窗口需要手动拉伸的情况。
- **查询页预览图片数量固定**：单页显示数量改为按可视区域动态计算，页面变宽或变高时自动增减每页图片数，不再出现大片空白或需要频繁翻页。
- **图片预览体验**：查询页打开图片预览后按原尺寸显示，支持鼠标滚轮缩放、按住拖拽平移、左右方向键（或按钮）切换上一张/下一张，Esc 键关闭。

### 变更

- **激活码工具独立存放**：激活码生成工具迁出项目脚本目录，集中到独立目录单独保存，不再随软件分发，也不随公开仓库发布。
- **代码加密方案**：新增源代码加密方案说明，记录需加密的文件范围、混淆与加固思路及实施步骤，仅供内部留存。

### 说明

- 本版本安装包：`xingqiyi-laundry-photo-setup-0.1.9.exe`（约 76.5 MB，Windows x64，NSIS 安装向导）。

## v0.1.8（2026-09-08）

### 修复

- **修复登录页无法登录、设置与版本号失效的问题**：v0.1.7 登录页存在一处对未定义函数的残留引用，导致登录页脚本在加载时中断，连带登录、服务器设置、版本号显示全部失效。已移除该残留引用，登录页恢复正常。（注：v0.1.7 安装包存在此问题，请直接使用 v0.1.8 及以上版本。）



### 新增

- **软件激活码 + 7 天试用**：首次安装后需输入激活码激活，未激活可试用 7 天；登录页「软件激活」弹窗显示本机机器码（与激活码一一绑定），试用到期后强制要求激活。配套提供管理员专用的「激活码计算工具」（`scripts/license-tool.js`），可根据客户机器码生成/校验激活码，两端算法共享 `main/license.js` 保证一致。
- **客户端灾难冗余（离线可用 + 自动同步）**：服务器失联时客户端可离线登录、拍照存档，操作进入本地离线队列；服务器恢复上线后自动回传同步，界面顶部显示离线状态与同步进度。
- **日志拆分记录**：同一码首张存档时，「新增条码号」与「新增存档照片」拆为两条独立操作日志，便于追溯。
- **按日期导出照片 + 条码归档 + 表格**：客户端支持按日期范围导出照片，自动按条码分文件夹归档，并附带一张表格（内容为「条码 + 文件位置」两列）；服务端本地复制、客户端从服务器拉取，导出计入操作日志。
- **登录界面一键下载安装包**：登录页展示当前版本号，并提供「一键下载安装包」按钮（无法访问 GitHub 时回退为打开下载页），店员未登录也能升级。
- **服务器端强制推送安装包**：管理端可开启强制推送并选择待推送安装包，客户端下次登录时自动检测并按提示安装。
- **客户端设置页一键下载安装包**：登录后在「设置」页新增「版本与更新」卡片，展示当前版本并支持一键下载/打开下载页、显示下载进度与取消。
- **操作日志显示操作 IP**：每条操作日志记录并展示来源 IP，管理端「数据总览」「操作日志」页均新增 IP 列，支持按 IP 搜索。
- **服务器端后台托盘常驻**：服务端模式下关闭窗口只隐藏到系统托盘，HTTP 服务持续运行，避免店员客户端连接失效；托盘菜单提供「显示主窗口」「退出程序」，并阻止系统休眠中断服务。客户端模式维持关窗即退出。

### 修复

- **输入框操作后失效无法点击**：原生对话框/资源管理器关闭后 Windows 可能只把焦点给到窗口边框而不给页面，导致输入框点不动；现窗口重新获得焦点时主动补交焦点，并对条码输入框增加存活守卫与「无焦点时才补焦」逻辑，渲染进程崩溃后自动重载恢复。
- **拍照偶发失焦**：新增对焦脉冲（定期 single-shot → continuous）并锁定画面中心对焦点，降低偶发失焦概率。

## v0.1.6（2026-09-04）

### 新增

- **更新弹窗与一键下载**：检测到新版本后自动弹出更新提示，弹窗展示当前版本的更新内容（来自 GitHub Releases 的发布说明），顶部横幅也可点击「查看并下载」重新唤起弹窗。
- **自动下载更新包**：点击「一键下载更新」后，安装包自动下载到本机「下载」目录的专属文件夹，实时显示下载进度（已下载/总大小、百分比），支持取消；下载完成后自动打开文件夹并定位安装包，双击安装即可覆盖升级。管理端「系统设置 → 版本与更新」页同样提供一键下载按钮。

### 变更

- 更新提示由单纯「前往下载页」改为「弹窗展示更新内容 + 一键下载」；无法连接 GitHub 的环境自动回退为打开下载页或联系管理员获取，不再提供自动下载。

## v0.1.5（2026-09-04）

### 新增

- **订单照片批量下载**：「记录查询 / 数据查看」页新增「按订单号批量下载」，勾选记录后一键导出，未勾选时导出当前列表全部条码；照片按条码（订单号）分文件夹保存到所选目录，文件名含拍摄时间与编号。服务端模式直接本地复制，客户端模式自动从服务器逐张拉取保存，导出结果计入操作日志。
- **全新软件 Logo**：登录页、启动设置页、侧边栏品牌区及窗口/任务栏图标统一更换为「星期衣」绿色品牌 Logo；安装包内嵌同图标。

## v0.1.4（2026-09-04）

### 修复

- **修复部分网络环境下检查不到新版本的问题**：
  - 更新检查改用系统网络通道（Windows 系统证书库），在代理或企业证书环境下不再出现证书验证失败导致无法连接 GitHub 的情况。
  - 不再依赖 GitHub「最新」接口的发布时间排序，改为遍历仓库全部正式发布、按版本号取最高者，避免标签格式或发布顺序异常时取错版本。

### 变更

- **安装包文件名改为英文**：产物统一为 `xingqiyi-laundry-photo-setup-版本号.exe`，避免中文文件名在上传、转发时出现乱码。

## v0.1.3（2026-09-04）

### 新增

- **安装时可选择安装位置**：安装包由一键安装调整为向导式安装，安装过程中可以自定义安装目录（默认仍为当前用户程序目录），适合需要安装到非系统盘（如 D 盘）的场景。

## v0.1.2（2026-09-04）

### 修复

- **修复局域网环境下其他电脑无法通过 IP 连接的问题**：排查确认此类问题主要由服务端电脑的 Windows 防火墙拦截入站连接导致。本版本在管理端「系统设置」页新增防火墙排查助手，可一键复制端口放行命令（在管理员终端执行即可放行）；同时更新文档，补充完整的局域网连接故障排查步骤。

### 变更

- **软件更新改为通过 GitHub Releases 分发**：
  - 应用启动与「检查更新」时，自动请求 GitHub Releases 接口获取最新版本信息；发现新版本后在界面顶部提示，并可一键打开下载页获取安装包。
  - 当无法访问 GitHub 时（如无外网环境），自动回退到原有「服务端更新文件夹」机制，保证任何环境下都有更新提示。
  - 管理端「系统设置 → 版本与更新」卡片相应调整：新增「前往下载页」按钮，原更新文件夹按钮保留为备用方案。

## v0.1.0

- 首个正式版本：客户端拍照/查询 + 管理端用户权限/日志/数据管理，支持局域网多机联网。
